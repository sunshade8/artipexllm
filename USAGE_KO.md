# EmpathyArtRAG 시스템 사용 가이드

이 문서는 EmpathyArtRAG 시스템을 실행하고 테스트하는 방법을 설명합니다.

## 사전 준비사항

시스템을 실행하기 전에 README_KO.md에 있는 설치 단계를 완료하고 다음 항목을 확인하세요:

1. `config.py`에 지정된 경로에 예술작품 메타데이터 CSV 파일이 있습니다
2. `config.py`에 지정된 경로에 예술작품 이미지 폴더가 있습니다
3. 필요한 모든 Python 종속성이 설치되어 있습니다

## 데모 실행하기

시스템에는 기능을 보여주는 CLI 데모 스크립트가 포함되어 있습니다:

```bash
# 예제 대화로 데모 실행하기
python demo.py

# 대화형 모드로 데모 실행하기
python demo.py --interactive
```

### 데모 옵션

- **예제 모드**: 시스템의 감정 감지 및 예술작품 추천 기능을 보여주는 미리 정의된 예제를 실행합니다.
- **대화형 모드**: 시스템과 자유롭게 대화하며 입력에 대한 응답을 테스트할 수 있습니다.

## 웹 애플리케이션 실행하기

시스템에는 간단한 웹 인터페이스도 포함되어 있습니다:

```bash
# 기본 호스트 및 포트(127.0.0.1:8000)에서 웹 애플리케이션 실행
python web_app.py

# 사용자 지정 호스트 및 포트로 웹 애플리케이션 실행
python web_app.py --host 0.0.0.0 --port 5000
```

### 웹 인터페이스 기능

웹 애플리케이션은 시스템과 상호 작용하는 더 사용자 친화적인 방법을 제공합니다:

1. 대화 기록이 있는 채팅 인터페이스
2. 사이드바에 표시되는 예술작품 추천
3. 페이지 새로고침 간에 대화 유지
4. 대화 기록을 지우는 옵션

## 감정 감지 테스트

다음과 같은 프롬프트로 감정 감지를 테스트해 보세요:

- "오늘은 정말 행복한 기분이에요!"
- "요즘 계속 슬픈 기분이고 어떻게 해야 할지 모르겠어요."
- "곧 있을 발표가 너무 불안해요."
- "오래된 사진을 볼 때면 향수가 느껴져요."
- "자연의 아름다움에 매료되었어요."

## 예술작품 추천 테스트

다음과 같은 프롬프트로 예술작품 추천 기능을 테스트해 보세요:

- "저에게 적합한 예술작품을 추천해 주시겠어요?"
- "슬픈 기분인데, 어떤 작품이 도움이 될까요?"
- "기쁨과 관련된 예술작품을 보여주세요."
- "마음을 진정시키기 위한 평화로운 예술작품이 필요해요."
- "경이로움과 경탄을 표현하는 예술작품은 무엇인가요?"

## 문제 해결

문제가 발생하는 경우:

1. **예술작품이 표시되지 않음**: `config.py`에서 예술 이미지 폴더 경로가 올바른지, 이미지가 객체 ID에 따라 이름이 지정되었는지 확인하세요.

2. **감정이 감지되지 않음**: 쿼리에 "행복한", "슬픈", "불안한" 등과 같은 명시적인 감정 단어를 사용해 보세요.

3. **시스템 오류**: 구성 문제를 나타낼 수 있는 오류 메시지가 콘솔 출력에 있는지 확인하세요.

4. **웹 인터페이스가 로드되지 않음**: FastAPI, Uvicorn, Jinja2 등 필요한 모든 종속성이 설치되어 있는지 확인하세요.

## 시스템 확장하기

EmpathyArtRAG의 모듈식 설계는 쉽게 확장할 수 있습니다:

- `emotion_handler.py`의 `DETECTABLE_EMOTIONS`에 새 감정 추가
- `answer_generator.py`의 `call_llm`을 수정하여 다른 LLM 공급자 통합
- CSV 파일을 업데이트하여 새 예술작품 데이터 추가
- `workflow.py`에서 추가 처리 노드로 워크플로우 그래프 확장 